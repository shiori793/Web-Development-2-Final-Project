// get user id from session storage and search data from local storage
$( window ).load(function() {
    // Run code
});

// get the value user inputted and add the amount in userInput object in localStorage
// update localStorage
function deposit(user_id, currency, amount) {

}